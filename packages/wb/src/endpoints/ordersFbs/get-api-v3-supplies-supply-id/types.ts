// Generated public types for this Wildberries endpoint.
/**
 * Параметры метода «Получить информацию о поставке».
 *
 * Метод возвращает подробную информацию о поставке.
 *
 * Лимит запросов (/openapi/api-information#tag/introduction/Limity-zaprosov) на один аккаунт продавца для методов сборочных заданий, поставок, пропусков и настроек автовозврата FBS:
 *
 * | Период | Лимит | Интервал | Всплеск |
 * | --- | --- | --- | --- |
 * | 1 мин | 300 запросов | 200 мс | 20 запросов |
 *
 * Один запрос с кодами ответов 4XX учитывается как 10 запросов
 *
 * GET /api/v3/supplies/{supplyId}
 *
 * Источник: https://dev.wildberries.ru/docs/openapi/orders-fbs
 */
export type GetApiV3SuppliesSupplyIdInput = {
  /**
   * Параметры пути.
   */
  readonly path: {
    /**
     * ID поставки
     *
     * Пример: `"WB-GI-1234567"`.
     */
    readonly supplyId: string;
  };
};
/**
 * Успешный ответ метода «Получить информацию о поставке».
 *
 * 200: Успешно.
 */
export type GetApiV3SuppliesSupplyIdResponse = {
  /**
   * ID поставки
   *
   * Пример: `"WB-GI-1234567"`.
   */
  readonly id?: string;

  /**
   * Признак B2B-продажи:
   *  - `true` — B2B-продажа
   *  - `false` — не B2B-продажа
   *  - `null` — признак отсутствует, сборочные задания не добавлены к поставке
   */
  readonly isB2b?: boolean | null;

  /**
   * Можно ли отгрузить заказ на ПВЗ:
   *  - `false` — нет
   *  - `true` — да
   *
   * Пример: `true`.
   */
  readonly isPickupPointShipmentAllowed?: boolean;

  /**
   * Флаг закрытия поставки:
   *  - `true` — закрыта
   *  - `false` — открыта
   */
  readonly done?: boolean;

  /**
   * Дата создания поставки (RFC3339)
   *
   * Формат: `date-time`.
   *
   * Пример: `"2022-05-04T07:56:29Z"`.
   */
  readonly createdAt?: string;

  /**
   * Дата закрытия поставки (RFC3339)
   *
   * Формат: `date-time`.
   *
   * Пример: `"2022-05-04T07:56:29Z"`.
   */
  readonly closedAt?: string | null;

  /**
   * Дата сканирования поставки или первого заказа (RFC3339)
   *
   * Формат: `date-time`.
   *
   * Пример: `"2022-05-04T07:56:29Z"`.
   */
  readonly scanDt?: string | null;

  /**
   * Наименование поставки
   *
   * Пример: `"Тестовая поставка"`.
   */
  readonly name?: string;

  /**
   * Тип товара:
   *  - `1` — малогабаритный товар (МГТ)
   *  - `2` — сверхгабаритный товар (СГТ)
   *  - `3` — крупногабаритный товар (КГТ+)
   */
  readonly cargoType?: 0 | 1 | 2 | 3;

  /**
   * Тип поставки:
   *  - `0` — внутренняя поставка
   *  - `1` — трансграничная поставка
   *  - `null` — значение отсутствует
   *
   * Формат: `int32`.
   *
   * Пример: `1`.
   */
  readonly crossBorderType?: (0 | 1) | null;

  /**
   * ID склада назначения поставки. Если `null`, склад назначения не указан
   *
   * Формат: `int64`.
   *
   * Пример: `123`.
   */
  readonly destinationOfficeId?: number | null;

  /**
   * ID рекомендуемого склада для приёмки поставки для Москвы и МО.
   *
   * Рекомендуется ближайший к покупателям склад, который определяется автоматически при передаче поставки в доставку с учётом параметров всех сборочных заданий в поставке.
   *
   * Если `0`, рекомендуемый склад не определён
   *
   * Формат: `int64`.
   *
   * Пример: `123569`.
   */
  readonly recommendedWhId?: number;
};
