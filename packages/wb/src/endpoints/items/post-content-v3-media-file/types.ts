// Generated public types for this Wildberries endpoint.
/**
 * Параметры метода «Загрузить медиафайл».
 *
 * Метод загружает и добавляет один медиафайл к карточке товара.
 *
 * Требования к изображениям:
 *  * максимум изображений для одной карточки товара — 30
 *  * минимальное разрешение — 700x900 px
 *  * максимальный размер — 32 Мб
 *  * минимальное качество — 65%
 *  * форматы — JPG, PNG, BMP, GIF (статичные), WebP
 *
 * Требования к видео:
 *  * максимум одно видео для одной карточки товара
 *  * максимальный размер — 50 Мб
 *  * форматы — MOV, MP4
 *
 * Лимит запросов (/openapi/api-information#tag/introduction/Limity-zaprosov) на один аккаунт продавца для всех методов Медиафайлов:
 *
 * | Тип | Период | Лимит | Интервал | Всплеск |
 * | --- | --- | --- | --- | --- |
 * | Персональный | 1 мин | 100 запросов | 600 мс | 5 запросов |
 * | Сервисный | 1 мин | 100 запросов | 600 мс | 5 запросов |
 * | Базовый с секретом | 1 мин | 100 запросов | 600 мс | 5 запросов |
 * | Базовый | 1 ч | 2 запроса | 30 мин | 1 запрос |
 *
 * POST /content/v3/media/file
 *
 * Источник: https://dev.wildberries.ru/docs/openapi/items
 */
export type PostContentV3MediaFileInput = {
  /**
   * Дополнительные HTTP-заголовки метода.
   */
  readonly headers: {
    /**
     * Артикул WB
     *
     * Пример: `"213864079"`.
     */
    readonly "X-Nm-Id": string;

    /**
     * Номер медиафайла на загрузку, начинается с `1`. При загрузке видео всегда указывайте `1`.
     *
     * Чтобы добавить изображение к уже загруженным, номер медиафайла должен быть больше количества уже загруженных медиафайлов.
     *
     * Пример: `2`.
     */
    readonly "X-Photo-Number": number;
  } /**
   * Тело запроса.
   */;
  readonly body: FormData;
};
/**
 * Успешный ответ метода «Загрузить медиафайл».
 *
 * 200: Успешно.
 */
export type PostContentV3MediaFileResponse = {
  /** Описание отсутствует в OpenAPI WB. */
  readonly data?: Readonly<Record<string, never>>;

  /**
   * Флаг ошибки
   */
  readonly error?: boolean;

  /**
   * Описание ошибки
   */
  readonly errorText?: string;

  /**
   * Дополнительные ошибки
   */
  readonly additionalErrors?: Readonly<Record<string, never>> | null;
};
