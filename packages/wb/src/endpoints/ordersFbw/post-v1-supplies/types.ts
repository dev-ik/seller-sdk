// Generated public types for this Wildberries endpoint.
import type { WbJsonValue } from "../../../schema.js";

/**
 * Параметры метода «Список поставок».
 *
 * Метод возвращает список поставок, по умолчанию — последние 1000 поставок.
 *
 * Лимит запросов (/openapi/api-information#tag/introduction/Limity-zaprosov) на один аккаунт продавца:
 *
 * | Тип | Период | Лимит | Интервал | Всплеск |
 * | --- | --- | --- | --- | --- |
 * | Персональный | 1 мин | 30 запросов | 2 сек | 10 запросов |
 * | Сервисный | 1 мин | 30 запросов | 2 сек | 10 запросов |
 * | Базовый с секретом | 1 мин | 30 запросов | 2 сек | 10 запросов |
 * | Базовый | 1 ч | 2 запроса | 30 мин | 1 запрос |
 *
 * POST /api/v1/supplies
 *
 * Источник: https://dev.wildberries.ru/docs/openapi/orders-fbw
 */
export type PostV1SuppliesInput = {
  /**
   * Параметры строки запроса.
   */
  readonly query?: {
    /**
     * Количество записей в ответе
     *
     * Значение по умолчанию: `1000`.
     *
     * Минимальное значение: 1.
     *
     * Максимальное значение: 1000.
     */
    readonly limit?: number;

    /**
     * После какого элемента выдавать данные
     *
     * Значение по умолчанию: `0`.
     */
    readonly offset?: number;
  } /**
   * Тело запроса.
   */;
  readonly body: {
    /**
     * Фильтр по датам
     */
    readonly dates?: readonly {
      /**
       * Дата начала периода
       *
       * Формат: `ISO 8601`.
       */
      readonly from?: string;

      /**
       * Дата окончания периода
       *
       * Формат: `ISO 8601`.
       */
      readonly till?: string;

      /**
       * Тип дат:
       *  - `factDate` — дата фактической отгрузки поставки
       *  - `createDate` — дата создания поставки
       *  - `supplyDate` — плановая дата отгрузки поставки
       *  - `updatedDate` — дата изменения поставки
       */
      readonly type: "factDate" | "createDate" | "supplyDate" | "updatedDate";
    }[];

    /**
     * Фильтр поставок по статусам. Возможные значения:
     *  - `1` — Не запланировано
     *  - `2` — Запланировано
     *  - `3` — Отгрузка разрешена
     *  - `4` — Идёт приёмка
     *  - `5` — Принято
     *  - `6` — Отгружено на воротах
     */
    readonly statusIDs?: readonly (1 | 2 | 3 | 4 | 5 | 6)[];
  };
};
/**
 * Успешный ответ метода «Список поставок».
 *
 * 200: Успешно.
 */
export type PostV1SuppliesResponse = readonly {
  /**
   * Телефон пользователя, создавшего поставку
   */
  readonly phone?: string;

  /**
   * ID поставки. Если `null`, это заказ, тогда используйте значение поля `preorderID`
   */
  readonly supplyID?: number | null;

  /**
   * ID заказа (незапланированная поставка). Для всех виртуальных поставок будет `0`
   */
  readonly preorderID?: number;

  /**
   * Дата и время создания поставки
   */
  readonly createDate?: string;

  /**
   * Плановая дата отгрузки поставки
   */
  readonly supplyDate?: string | null;

  /**
   * Дата фактической отгрузки поставки
   */
  readonly factDate?: string | null;

  /**
   * Дата изменения поставки
   */
  readonly updatedDate?: string | null;

  /**
   * ID статуса поставки:
   *  - `1` — Не запланировано
   *  - `2` — Запланировано
   *  - `3` — Отгрузка разрешена
   *  - `4` — Идёт приёмка
   *  - `5` — Принято
   *  - `6` — Отгружено на воротах
   */
  readonly statusID?: 1 | 2 | 3 | 4 | 5 | 6;

  /**
   * ID типа поставки:
   *  - `0` — Без коробов (виртуальная поставка)
   *  - `1` и `2` — Короба
   *  - `5` — Монопаллеты
   *  - `6` — Суперсейф
   */
  readonly boxTypeID?: WbJsonValue;

  /**
   * Тип поставки — **Поштучная палета**:
   *  - `true` — да
   *  - `false` — нет
   *
   *  Поле возвращается только при `"boxTypeID": 2`
   */
  readonly isBoxOnPallet?: boolean;
}[];
