import { object, type Schema } from "@safe-shape/core";
import type { ApproveFbsCarriageResponse } from "./types.js";

export const approveFbsCarriageResponseSchema: Schema<ApproveFbsCarriageResponse> =
  object({});
