/** Булев результат операции изменения жизненного цикла товара. */
export interface OzonProductLifecycleBooleanResponse {
  /** true, если запрос обработан без ошибок. */
  readonly result?: boolean;
}
