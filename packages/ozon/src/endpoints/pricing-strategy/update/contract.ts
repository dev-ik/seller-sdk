import { object, type Schema } from "@safe-shape/core";
import type { UpdatePricingStrategyResponse } from "./types.js";

export const updatePricingStrategyResponseSchema: Schema<UpdatePricingStrategyResponse> =
  object({});
