import { object, type Schema } from "@safe-shape/core";
import type { DeletePricingStrategyResponse } from "./types.js";

export const deletePricingStrategyResponseSchema: Schema<DeletePricingStrategyResponse> =
  object({});
