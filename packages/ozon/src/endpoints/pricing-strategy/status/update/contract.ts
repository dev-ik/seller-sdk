import { object, type Schema } from "@safe-shape/core";
import type { UpdatePricingStrategyStatusResponse } from "./types.js";

export const updatePricingStrategyStatusResponseSchema: Schema<UpdatePricingStrategyStatusResponse> =
  object({});
