import { object, type Schema } from "@safe-shape/core";
import type { UpdateReviewStatusV1Response } from "./types.js";

export const updateReviewStatusV1ResponseSchema: Schema<UpdateReviewStatusV1Response> =
  object({});
